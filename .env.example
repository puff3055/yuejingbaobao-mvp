# Never place a real key in the browser bundle or commit it to Git.
AGENT_API_BASE_URL=https://provider.example/v1
AGENT_API_MODEL=provider-model-name
AGENT_API_KEY=replace-in-deployment-secret-store
